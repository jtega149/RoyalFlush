services:
  server:
    image: royalflush-server:v1
    container_name: royalflush-server
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3001:3001"
    restart: unless-stopped